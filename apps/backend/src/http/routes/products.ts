import { FastifyInstance } from 'fastify';

// Rotas de produtos - a serem implementadas na próxima fase
export async function productRoutes(fastify: FastifyInstance) {
  // GET /api/products/search
  // GET /api/products/:id
  // A implementar
}

